-- The script creates the hbtn_0c_0 database on the MySQL server if it doesn't exist.
/*
   - CREATE DATABASE: Tells MySQL you want to create a database.
   - IF NOT EXISTS: Makes sure the creation doesn't fail if it already exists.
   - hbtn_0c_0: The name of the database you want to create.
*/
CREATE DATABASE IF NOT EXISTS hbtn_0c_0;
